<Window x:Class="SimpleScreenshoter.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Simple Screenshoter - by Payziii" Height="500" Width="600" WindowStyle="None" Background="#161616" AllowsTransparency="True">
    <Border Background="#161616" CornerRadius="10" Padding="2">
        <DockPanel LastChildFill="True">
            <Border Background="#202020" Height="40" DockPanel.Dock="Top" x:Name="TitleBar" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Simple Screenshoter - by Payziii" Foreground="White" VerticalAlignment="Center" Margin="10,0" TextAlignment="Center"/>

                    <Button Content="—" Width="30" Height="30" Margin="0,0,5,0" Grid.Column="1"
        Background="Transparent" BorderThickness="0" Foreground="White"
        FontSize="16" FontWeight="Bold"
        Click="MinimizeButton_Click"/>
                    <Button Content="▢" Width="30" Height="30" Margin="0,0,5,0" Grid.Column="2"
        Background="Transparent" BorderThickness="0" Foreground="White"
        FontSize="16" FontWeight="Bold"
        Click="MaximizeRestoreButton_Click"/>
                    <Button Content="X" Width="30" Height="30" Grid.Column="3"
        Background="Transparent" BorderThickness="0" Foreground="White"
        FontSize="16" FontWeight="Bold"
        Click="CloseButton_Click"/>
                </Grid>
            </Border>
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" Padding="10">
                <StackPanel Orientation="Horizontal" Width="570">
                    <Border CornerRadius="20" Background="#202020" Width="250" Height="208" Margin="50" Padding="10">
                        <Grid>
                            <Image Source="/firefox_3Jwz4W5ww4.png" ClipToBounds="True" Margin="0,10,0,67" Stretch="UniformToFill"/>
                            <TextBlock Text="13.08.2025 14:45" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,153,0,0" FontSize="18"/>
                        </Grid>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </DockPanel>
    </Border>
</Window>
